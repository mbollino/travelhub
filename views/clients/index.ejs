<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= user.username %>'s Clients</title>
</head>
<body>
    <%- include('../partials/_navbar.ejs') %>
    <h1><%= user.username %>'s Clients</h1>
    <a href="/users/<%= user._id %>/clients/new">Add a Client</a>
    <ul>
        <% clients.forEach((client) => { %>
        <li>
            <a href="/users/<%= user._id %>/clients/<%= client._id %>">
            Depart Date: <%= new Date(client.departDate).toISOString().split('T')[0] %> <%= client.firstName %> <%= client.lastName %>
            </a>
        </li>
        <% }) %>
    </ul>
</body>
</html>