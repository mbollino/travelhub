<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= client.firstName %> <%= client.lastName %></title>
</head>
<body>
    
    <h1><%= client.firstName %> <%= client.lastName %></h1>
    <p>Phone Number: <% const phoneNumber = client.phoneNumber; 
        const formattedPhoneNumber = `(${phoneNumber.slice(0, 3)}) ${phoneNumber.slice(3, 6)}-${phoneNumber.slice(6, 10)}` %>
        <%= formattedPhoneNumber %> <%= client.category %>
        <% if (client.phonePreferred === true) { %>
            <%= "Preferred Contact" %>
        <% } %></p>
    <p>Email: <%=client.email%></p>
         <% if (client.emailPreferred === true) { %> 
        <%= "Preferred Contact" %>
    <% } %></p>
    <p>Depart Date: <%= client.departDate.toLocaleDateString('en-US', {year: 'numeric', month: '2-digit', day: '2-digit' }) %></p>
    <p>Return Date: <%= client.returnDate.toLocaleDateString('en-US', {year: 'numeric', month: '2-digit', day: '2-digit' }) %></p>
    <p>Destination: <%= client.destination %></p>
    <% if (client.hotel) { %> 
        <p>Hotel: <%= client.hotel %></p>
    <% } %> 
    <% if (client.airline) { %>
        <p>Airline: <%= client.airline %></p>
    <% } %>   
    <p>Car Rental: <%= client.carRentalRequired %></p>
    <p>Current Status: <%= client.status %></p>
    <% if (client.notes) { %>
    <p>Notes: <%= client.notes %></p>
    <% } %>
</body>
</html>